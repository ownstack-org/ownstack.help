{
  "name": "ownstack-help",
  "private": true,
  "version": "0.1.0",
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "build": "node build.js",
    "heroku-postbuild": "npm run build",
    "start": "serve -l tcp://0.0.0.0:${PORT:-3000} ."
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
